# Security policy

Do not report suspected vulnerabilities in a public issue. Use the repository's
private GitHub security advisory flow or email `feedback@tensorplay.cn`.

These Skills must never instruct an agent to place tokens, provider keys, Space
secrets, or reviewer credentials in prompts, tool arguments, logs, or source.
